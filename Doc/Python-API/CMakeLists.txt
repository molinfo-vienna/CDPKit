IF(DOXYGEN_EXECUTABLE)
  #SET(DOXYGEN_HTML_STYLESHEET "${CMAKE_CURRENT_SOURCE_DIR}/stylesheet.css")
  SET(DOXYGEN_HTML_STYLESHEET "")
  CONFIGURE_FILE(Doxyfile.in "${CMAKE_CURRENT_BINARY_DIR}/Doxyfile" @ONLY)
  CONFIGURE_FILE("${CDPKIT_CMAKE_SCRIPTS_DIR}/PostprocessPythonAPIDoc.cmake.in" "${CMAKE_CURRENT_BINARY_DIR}/PostprocessPythonAPIDoc.cmake" @ONLY)

  ADD_CUSTOM_TARGET(python-api-doc "${DOXYGEN_EXECUTABLE}" 
                    COMMAND "${CMAKE_COMMAND}" -P "${CMAKE_CURRENT_BINARY_DIR}/PostprocessPythonAPIDoc.cmake"
                    WORKING_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}")
ENDIF(DOXYGEN_EXECUTABLE)
