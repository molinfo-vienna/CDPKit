FILE(GLOB_RECURSE HTML_FILES "@CMAKE_CURRENT_BINARY_DIR@/*.html")

FOREACH(HTML_FILE ${HTML_FILES})
  FILE(READ "${HTML_FILE}" FILE_CONTENTS)
  STRING(REPLACE "Namespace" "Package" FILE_CONTENTS "${FILE_CONTENTS}")
  file(WRITE "${HTML_FILE}" "${FILE_CONTENTS}")
ENDFOREACH(HTML_FILE)
